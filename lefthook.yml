# ==============================================================================================
# Run this command if use ssh protocol:
#  git config --global url."git@gitrepo.xlaxiata.id:".insteadOf "https://gitrepo.xlaxiata.id/"
#
# Clear cache
# Windows
#  Remove-Item -Recurse -Force .git\info\lefthook-remotes
#  Remove-Item -Force .git\info\lefthook.checksum
#
# MacOS
#  rm -rf .git/info/lefthook-remotes
#  rm -f .git/info/lefthook.checksum
#
# ==============================================================================================

remotes:
  - git_url: https://gitrepo.xlaxiata.id/anthos/tools/lefthook-config.git
    ref: master
    configs:
      - lefthook-remote.yml